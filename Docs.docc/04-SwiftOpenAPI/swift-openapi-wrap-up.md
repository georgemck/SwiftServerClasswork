# Wrap up

@Metadata {
    @PageKind(article)
    @PageColor(blue)
    @SupportedLanguage(swift)
}

Check that your `GET` endpoint conforms to what you defined in the OpenAPI document (an empty array).

## Verify solutions

While the server is running, run this `curl` command to test your solutions:

```bash
curl -i http://127.0.0.1:8080/api/polls
```

## Additional resources

To further expand your knowledge of REST API design and data handling, review [REST API design best
practices](https://restfulapi.net) for comprehensive endpoint patterns.
