# Wrap up

@Metadata {
    @PageKind(article)
    @PageColor(blue)
    @SupportedLanguage(swift)
}

Test your two endpoints with curl.

## Verify solutions

You can test your two solutions with `curl`. You may need to revisit the poll creation process first.
